<view class="manage-container">
  <view class="goods-list">
    <view wx:for="{{products}}" wx:key="id" class="goods-card" bindtap="goEdit" data-id="{{item.id}}" data-product="{{item}}">
      <view class="goods-content">
        <image class="goods-img" src="{{item.coverUrl || 'https://img.yzcdn.cn/vant/cat.jpeg'}}" mode="aspectFill"/>
        <view class="goods-title">{{item.name}}</view>
        <view class="goods-meta">
          <view class="goods-price">￥{{item.price}}</view>
        </view>
        <view class="status-row">
          <view class="status {{item.status === 'PUBLISHED' ? 'on' : 'off'}}">{{item.status === 'PUBLISHED' ? '已上架' : '已下架'}}</view>
          <view class="toggle-btn" catchtap="toggleStatus" data-id="{{item.id}}" data-status="{{item.status}}">{{item.status === 'PUBLISHED' ? '下架' : '上架'}}</view>
        </view>
      </view>
    </view>
    <view wx:if="{{!products.length}}" class="empty-tip">暂无发布的商品</view>
  </view>
</view>

