<view class="chat-container">
  <!-- 标题栏 -->
  <view class="chat-header">
    <view class="back-btn" bindtap="goBack">
      <text>←</text>
    </view>
    <view class="chat-title"></view>
    <view class="header-spacer"></view>
  </view>

  <!-- 消息列表 -->
  <scroll-view 
    class="messages-list" 
    scroll-y 
    scroll-into-view="{{toView}}"
    scroll-top="{{scrollTop}}"
    bindscrolltoupper="loadMore"
  >
    <view class="messages-inner">
      <view wx:for="{{messages}}" wx:key="index" class="message-item {{item.userId === currentUserId ? 'sent' : 'received'}}">
        <!-- 接收的消息 -->
        <view wx:if="{{item.userId !== currentUserId}}" class="received-message">
          <image wx:if="{{item.avatarUrl}}" src="{{item.avatarUrl}}" class="avatar" mode="aspectFill" />
          <view wx:else class="avatar-placeholder">👤</view>
          
          <view class="message-content">
            <view class="nickname">{{item.nickname}}</view>
            <view class="bubble received-bubble" bindlongpress="onLongPressMessage" data-message-id="{{item.id}}" data-content="{{item.content}}" data-user-id="{{item.userId}}">
              {{item.content}}
            </view>
            <view wx:if="{{item.translatedText}}" class="translated-text">
              {{item.translatedText}}
            </view>
            <view class="time">{{formatTime(item.createdAt)}}</view>
          </view>
        </view>
        
        <!-- 发送的消息 -->
        <view wx:if="{{item.userId === currentUserId}}" class="sent-message">
          <view class="message-content">
            <view class="bubble sent-bubble" bindlongpress="onLongPressMessage" data-message-id="{{item.id}}" data-content="{{item.content}}" data-user-id="{{item.userId}}">
              {{item.content}}
            </view>
            <view wx:if="{{item.translatedText}}" class="translated-text">
              {{item.translatedText}}
            </view>
            <view class="time">{{formatTime(item.createdAt)}}</view>
          </view>
          
          <image wx:if="{{currentUserAvatar}}" src="{{currentUserAvatar}}" class="avatar" mode="aspectFill" />
          <view wx:else class="avatar-placeholder">👤</view>
        </view>
      </view>
      
      <!-- 加载更多提示 -->
      <view wx:if="{{messages.length === 0}}" class="empty-hint">
        <text>暂无消息，开始聊天吧～</text>
      </view>
    </view>
  </scroll-view>

  <!-- 输入框 -->
  <view class="input-area">
    <view wx:if="{{replyingTo}}" class="reply-info">
      <text class="reply-label">回复 @{{replyingToNickname}}</text>
      <text class="reply-close" bindtap="cancelReply">×</text>
    </view>
    <view class="input-wrapper">
      <input 
        type="text" 
        class="input-box" 
        placeholder="输入消息..." 
        placeholder-class="input-placeholder"
        bindinput="onInput"
        value="{{inputContent}}"
        bindconfirm="sendMessage"
      />
      <button class="send-btn" bindtap="sendMessage">发送</button>
    </view>
  </view>

  <!-- 长按菜单 -->
  <view wx:if="{{showContextMenu}}" class="context-menu" bindtap="closeContextMenu">
    <view class="context-menu-content" catchtap="">
      <button class="context-menu-item" bindtap="replyMessage">回复</button>
      <button class="context-menu-item" bindtap="translateMessage">翻译</button>
      <button class="context-menu-item" bindtap="speakMessage">朗诵</button>
      <button class="context-menu-item" bindtap="copyMessage">复制</button>
      <button class="context-menu-item cancel" bindtap="closeContextMenu">取消</button>
    </view>
  </view>
</view>
