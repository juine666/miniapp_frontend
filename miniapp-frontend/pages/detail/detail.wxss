.container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: #f8f9ff;
}

.loading-container {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  font-size: 32rpx;
  color: #999;
}

.content {
  flex: 1;
  overflow-y: auto;
  overflow-x: hidden;
}

/* 图片区域 */
.image-section {
  position: relative;
  width: 100%;
  height: 400rpx;
  background: #f0f1ff;
}

.image-swiper {
  width: 100%;
  height: 100%;
}

.main-image {
  width: 100%;
  height: 100%;
  cursor: pointer;
}

.image-indicator {
  position: absolute;
  bottom: 16rpx;
  right: 16rpx;
  background: rgba(0, 0, 0, 0.5);
  color: #fff;
  padding: 6rpx 12rpx;
  border-radius: 999rpx;
  font-size: 24rpx;
  z-index: 10;
}

/* 右下角按钮 */
.comment-btn {
  position: absolute;
  bottom: 16rpx;
  right: 16rpx;
  width: 90rpx;
  height: 90rpx;
  border-radius: 50%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  font-size: 45rpx;
  background: rgba(255, 255, 255, 0.9);
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  z-index: 11;
  transition: all 0.3s;
  gap: 2rpx;
}

.favorite-btn:active,
.comment-btn:active {
  transform: scale(0.9);
}

.favorite-btn.favorited {
  background: rgba(255, 107, 107, 0.9);
  color: #fff;
}

.comment-icon {
  font-size: 28rpx;
}

.comment-count {
  font-size: 20rpx;
  color: #ff4757;
  font-weight: 600;
}

/* 商品信息 */
.info-section {
  background: #fff;
  padding: 24rpx;
  margin-bottom: 16rpx;
}

.title {
  font-size: 36rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 12rpx;
  line-height: 1.5;
}

.description {
  font-size: 28rpx;
  color: #666;
  margin-bottom: 20rpx;
  line-height: 1.6;
}

.meta-row {
  display: flex;
  align-items: center;
  margin-bottom: 12rpx;
  font-size: 26rpx;
}

.meta-label {
  color: #999;
  margin-right: 12rpx;
  min-width: 100rpx;
}

.meta-value {
  color: #333;
  flex: 1;
}

/* 卖家信息 */
.seller-section {
  background: #fff;
  padding: 24rpx;
  margin-bottom: 16rpx;
}

.seller-header {
  display: flex;
  align-items: center;
  margin-bottom: 20rpx;
  gap: 16rpx;
}

.seller-avatar {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  flex-shrink: 0;
}

.seller-info {
  flex: 1;
}

.seller-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 4rpx;
}

.seller-tag {
  font-size: 24rpx;
  color: #999;
}

.contact-btn,
.edit-btn {
  background: #667eea;
  color: #fff;
  border: none;
  padding: 10rpx 20rpx;
  border-radius: 20rpx;
  font-size: 24rpx;
  font-weight: 600;
  flex-shrink: 0;
}

.seller-contact {
  border-top: 1rpx solid #f0f0f0;
  padding-top: 16rpx;
}

.contact-item {
  display: flex;
  align-items: center;
  margin-bottom: 12rpx;
  gap: 12rpx;
  font-size: 26rpx;
}

.contact-label {
  color: #999;
  min-width: 80rpx;
}

.contact-value {
  color: #333;
  flex: 1;
}

.copy-btn {
  background: #f0f0f0;
  color: #666;
  padding: 6rpx 12rpx;
  border-radius: 12rpx;
  font-size: 22rpx;
}

/* 语音体验区 */
.voices-section {
  background: #fff;
  padding: 24rpx;
  margin-bottom: 40rpx;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

.add-btn {
  background: #ffd93d;
  color: #333;
  border: none;
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  font-size: 24rpx;
  font-weight: 600;
}

.voice-input-box {
  background: #f8f9ff;
  padding: 16rpx;
  border-radius: 12rpx;
  margin-bottom: 16rpx;
}

.recording-status {
  text-align: center;
  margin-bottom: 12rpx;
  color: #666;
  font-size: 26rpx;
}

.status-text {
  color: #999;
}

.recording-text {
  color: #ff4757;
  font-weight: 600;
}

.recording-controls {
  display: flex;
  gap: 12rpx;
  justify-content: center;
}

.record-btn {
  background: #667eea;
  color: #fff;
  border: none;
  padding: 12rpx 24rpx;
  border-radius: 20rpx;
  font-size: 26rpx;
  font-weight: 600;
  flex: 1;
}

.record-btn.stop {
  background: #ff4757;
}

.voices-list {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.empty-tip {
  text-align: center;
  color: #999;
  font-size: 26rpx;
  padding: 40rpx 0;
}

.voice-item {
  display: flex;
  gap: 12rpx;
  background: #f8f9ff;
  padding: 12rpx;
  border-radius: 12rpx;
}

.avatar {
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  flex-shrink: 0;
}

.voice-content {
  flex: 1;
  font-size: 26rpx;
}

.user-name {
  font-weight: 600;
  color: #333;
  margin-bottom: 4rpx;
}

.voice-time {
  color: #999;
  font-size: 22rpx;
  margin-bottom: 8rpx;
}

.voice-player {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-bottom: 8rpx;
}

.label {
  color: #666;
  font-size: 24rpx;
}

.play-btn {
  background: #667eea;
  color: #fff;
  border: none;
  padding: 4rpx 12rpx;
  border-radius: 12rpx;
  font-size: 22rpx;
}

.text-content {
  color: #666;
  font-size: 26rpx;
  line-height: 1.5;
  padding: 8rpx 0;
}

/* Modal */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: flex-end;
  z-index: 1000;
}

.modal-content {
  width: 100%;
  height: 80vh;
  background: #fff;
  border-radius: 20rpx 20rpx 0 0;
  display: flex;
  flex-direction: column;
  animation: slideUp 0.3s ease;
  padding-bottom: env(safe-area-inset-bottom);
}

@keyframes slideUp {
  from {
    transform: translateY(100%);
  }
  to {
    transform: translateY(0);
  }
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 24rpx;
  border-bottom: 1rpx solid #f0f0f0;
}

.modal-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

.modal-close {
  font-size: 36rpx;
  color: #999;
  cursor: pointer;
}

/* 评论输入框 - 固定底部 */
.comment-input-footer {
  background: #fff;
  border-top: 1rpx solid #f0f0f0;
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  gap: 0;
  border-radius: 0;
  padding: 0;
  padding-bottom: env(safe-area-inset-bottom);
}

.comment-input-box {
  background: #fff;
  padding: 16rpx 24rpx;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  box-sizing: border-box;
}

.emotion-bar {
  margin-bottom: 12rpx;
}

.emotion-label {
  font-size: 26rpx;
  color: #666;
  margin-right: 8rpx;
}

.emotion-list {
  display: flex;
  gap: 8rpx;
  flex-wrap: wrap;
  margin-top: 8rpx;
}

.emotion-btn {
  padding: 6rpx 12rpx;
  background: #f0f0f0;
  border-radius: 12rpx;
  font-size: 28rpx;
  border: 2rpx solid transparent;
}

.emotion-btn.selected {
  border-color: #667eea;
  background: rgba(102, 126, 234, 0.1);
}

.comment-input {
  width: 100%;
  border: 1rpx solid #f0f0f0;
  border-radius: 12rpx;
  padding: 12rpx;
  font-size: 26rpx;
  margin-bottom: 12rpx;
  min-height: 80rpx;
  box-sizing: border-box;
}

/* 富文本编辑器 */
.rich-editor-wrapper {
  margin-bottom: 12rpx;
}

.rich-editor {
  width: 100%;
  border: 1rpx solid #e0e0e0;
  border-radius: 12rpx;
  padding: 12rpx;
  font-size: 26rpx;
  min-height: 150rpx;
  background: #fafafa;
  box-sizing: border-box;
}

.editor-toolbar {
  display: flex;
  gap: 12rpx;
  padding: 12rpx 0;
  border-top: 1rpx solid #f0f0f0;
  margin-top: 12rpx;
}

.toolbar-group {
  display: flex;
  gap: 8rpx;
  border-right: 1rpx solid #f0f0f0;
  padding-right: 12rpx;
}

.toolbar-group:last-child {
  border-right: none;
  padding-right: 0;
}

.toolbar-btn {
  width: 40rpx;
  height: 40rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #f0f0f0;
  border-radius: 8rpx;
  font-size: 20rpx;
  font-weight: 600;
  color: #666;
  cursor: pointer;
  transition: all 0.2s;
}

.toolbar-btn:active {
  background: #e0e0e0;
  transform: scale(0.95);
}

.input-actions {
  display: flex;
  gap: 12rpx;
}

.cancel-btn,
.submit-btn {
  flex: 1;
  border: none;
  padding: 10rpx;
  border-radius: 12rpx;
  font-size: 26rpx;
  font-weight: 600;
}

.cancel-btn {
  background: #f0f0f0;
  color: #666;
}

.submit-btn {
  background: #667eea;
  color: #fff;
}

.comments-list {
  flex: 1;
  overflow-y: auto;
  padding: 16rpx 24rpx;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  min-height: 200rpx;
}

.comment-item {
  display: flex;
  gap: 12rpx;
  background: #f8f9ff;
  padding: 12rpx;
  border-radius: 12rpx;
  position: relative;
}

.comment-main {
  flex: 1;
  font-size: 26rpx;
}

.user-name {
  font-weight: 600;
  color: #333;
  margin-bottom: 4rpx;
}

.comment-text {
  color: #666;
  margin-bottom: 8rpx;
  line-height: 1.5;
  word-break: break-all;
}

.translate-box {
  background: linear-gradient(135deg, #e8eaf6 0%, #f3e5f5 100%);
  padding: 16rpx 14rpx;
  border-radius: 12rpx;
  margin: 12rpx 0;
  border-left: 4rpx solid #667eea;
  box-shadow: 0 2rpx 8rpx rgba(102, 126, 234, 0.08);
}

.translate-header {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 10rpx;
}

.translate-label {
  font-size: 20rpx;
  color: #667eea;
  font-weight: 700;
  flex-shrink: 0;
  letter-spacing: 0.5rpx;
}

.play-voice-btn {
  background: #667eea;
  color: #fff;
  border: none;
  padding: 6rpx 12rpx;
  border-radius: 8rpx;
  font-size: 18rpx;
  flex-shrink: 0;
  transition: all 0.2s;
}

.play-voice-btn:active {
  background: #556dd4;
  transform: scale(0.95);
}

.translate-text {
  color: #555;
  font-size: 24rpx;
  line-height: 1.6;
  word-break: break-all;
  display: block;
  font-weight: 500;
}

.comment-meta {
  display: flex;
  gap: 12rpx;
  font-size: 22rpx;
  color: #999;
  margin-top: 8rpx;
}

.like-btn {
  cursor: pointer;
  color: #ff4757;
}

.translate-btn {
  cursor: pointer;
  color: #667eea;
  padding: 0 8rpx;
}

.translate-voice-btn {
  cursor: pointer;
  color: #ff6b6b;
  padding: 0 8rpx;
}

/* 长按菜单 */
.context-menu {
  position: absolute;
  background: rgba(0, 0, 0, 0.9);
  color: #fff;
  border-radius: 8rpx;
  z-index: 100;
  min-width: 120rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.2);
}

.menu-item {
  padding: 12rpx 16rpx;
  font-size: 24rpx;
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.1);
  text-align: center;
}

.menu-item:last-child {
  border-bottom: none;
}

/* 评论输入框 - 固定底部 */
.comment-input-footer {
  background: #fff;
  border-top: 1rpx solid #f0f0f0;
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  gap: 0;
  border-radius: 0;
  padding: 0;
  padding-bottom: env(safe-area-inset-bottom);
}

.emotion-bar {
  display: flex;
  align-items: center;
  margin-bottom: 12rpx;
  padding: 12rpx 0;
  border-bottom: 1rpx solid #f0f0f0;
}

.emotion-label {
  font-size: 24rpx;
  color: #999;
  margin-right: 12rpx;
  min-width: 60rpx;
}

.emotion-list {
  display: flex;
  gap: 8rpx;
  flex-wrap: wrap;
  flex: 1;
}

.emotion-btn {
  font-size: 28rpx;
  padding: 8rpx 12rpx;
  border-radius: 6rpx;
  transition: all 0.2s;
  cursor: pointer;
  background: #f0f0f0;
  border: 2rpx solid transparent;
}

.emotion-btn.selected {
  background: #667eea;
  color: #fff;
  border-color: #667eea;
  transform: scale(1.1);
}

/* 输入模式选项卡 */
.input-mode-tabs {
  display: flex;
  gap: 0;
  margin-bottom: 16rpx;
  border-bottom: 1rpx solid #f0f0f0;
}

.input-mode-tabs .tab {
  flex: 1;
  padding: 12rpx;
  text-align: center;
  font-size: 26rpx;
  color: #999;
  border-bottom: 3rpx solid transparent;
  transition: all 0.3s;
}

.input-mode-tabs .tab.active {
  color: #667eea;
  border-bottom-color: #667eea;
  font-weight: 600;
}

/* 文字输入框 */
.comment-input {
  width: 100%;
  min-height: 100rpx;
  padding: 12rpx;
  border: 1rpx solid #f0f0f0;
  border-radius: 8rpx;
  font-size: 28rpx;
  line-height: 1.6;
  margin-bottom: 12rpx;
  box-sizing: border-box;
}

/* 语音输入区域 */
.voice-input-section {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  padding: 16rpx;
  background: #f8f9ff;
  border-radius: 8rpx;
  margin-bottom: 12rpx;
}

/* 语音录制按钮 */
.voice-record-btn {
  width: 100%;
  padding: 32rpx 24rpx;
  background: #667eea;
  color: #fff;
  border: none;
  border-radius: 8rpx;
  font-size: 28rpx;
  font-weight: 600;
  transition: all 0.3s;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
}

.voice-record-btn.recording {
  background: #ff4757;
  animation: pulse 1s infinite;
}

@keyframes pulse {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.7;
  }
}

.record-text {
  display: block;
}

/* 语音识别预览 */
.voice-preview {
  background: #fff;
  padding: 12rpx;
  border-radius: 6rpx;
  border-left: 3rpx solid #667eea;
}

.preview-label {
  display: block;
  font-size: 22rpx;
  color: #999;
  margin-bottom: 6rpx;
}

.preview-text {
  display: block;
  font-size: 26rpx;
  color: #333;
  line-height: 1.6;
}

/* 输入框操作按钮 */
.input-actions {
  display: flex;
  gap: 12rpx;
  padding-top: 12rpx;
  border-top: 1rpx solid #f0f0f0;
  flex-shrink: 0;
  position: sticky;
  bottom: 0;
  background: #fff;
}

.cancel-btn,
.submit-btn {
  flex: 1;
  padding: 12rpx 16rpx;
  border: none;
  border-radius: 6rpx;
  font-size: 26rpx;
  font-weight: 600;
  transition: all 0.2s;
}

.cancel-btn {
  background: #f0f0f0;
  color: #999;
}

.cancel-btn:active {
  background: #e0e0e0;
}

.submit-btn {
  background: #667eea;
  color: #fff;
}

.submit-btn:active {
  background: #556dd4;
}

/* 二级回复列表 */
.replies-section {
  padding-left: 56rpx;
  margin-top: 8rpx;
  margin-bottom: 12rpx;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.reply-item {
  display: flex;
  gap: 12rpx;
  background: #f8f9ff;
  padding: 14rpx 16rpx;
  border-radius: 10rpx;
  margin-bottom: 10rpx;
  border-left: none;
}

.reply-item .avatar {
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  flex-shrink: 0;
}

.reply-item .comment-main {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.reply-item .user-name {
  font-size: 24rpx;
  font-weight: 500;
  color: #333;
  display: block;
}

.reply-item .comment-text {
  font-size: 24rpx;
  color: #666;
  line-height: 1.5;
  word-break: break-all;
  display: block;
}

.reply-item .comment-meta {
  font-size: 20rpx;
  color: #999;
  display: flex;
  gap: 12rpx;
  flex-wrap: wrap;
  margin-top: 2rpx;
}

/* 展开/折叠回复按钮 */
.expand-replies-btn {
  padding: 12rpx 0;
  color: #667eea;
  font-size: 24rpx;
  font-weight: 500;
  cursor: pointer;
  margin-left: 56rpx;
  transition: all 0.3s;
}

.expand-replies-btn:active {
  opacity: 0.7;
}

.expand-replies-btn.collapse {
  color: #999;
}

/* 三级回复列表 */
.third-level-replies {
  padding-left: 0;
  margin-top: 0;
  margin-bottom: 8rpx;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.third-reply-item {
  display: flex;
  gap: 12rpx;
  background: #f5f6ff;
  padding: 12rpx 14rpx;
  border-radius: 8rpx;
  margin-bottom: 8rpx;
}

.third-reply-item .avatar {
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  flex-shrink: 0;
}

.third-reply-item .comment-main {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.third-reply-item .user-name {
  font-size: 22rpx;
  font-weight: 500;
  color: #333;
  display: block;
}

.third-reply-item .comment-text {
  font-size: 22rpx;
  color: #666;
  line-height: 1.4;
  word-break: break-all;
  display: block;
}

.third-reply-item .comment-meta {
  font-size: 18rpx;
  color: #999;
  display: flex;
  gap: 12rpx;
  flex-wrap: wrap;
  margin-top: 2rpx;
}

.reply-to {
  color: #999;
  font-weight: normal;
}

/* 聊天室入口 */
.chat-room-section {
  background: #fff;
  padding: 24rpx;
  margin-bottom: 16rpx;
}

.chat-room-btn {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: #fff;
  border: none;
  padding: 16rpx 24rpx;
  border-radius: 12rpx;
  font-size: 28rpx;
  font-weight: 600;
  transition: all 0.3s;
}

.chat-room-btn:active {
  opacity: 0.9;
  transform: scale(0.98);
}

.chat-icon {
  font-size: 32rpx;
}

.chat-text {
  font-size: 28rpx;
}
